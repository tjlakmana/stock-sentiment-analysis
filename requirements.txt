# ============================================================
# Stock Sentiment Analysis — Python Dependencies
# Python 3.11+ required
# ============================================================

# Environment
python-dotenv>=1.0.0

# HTTP
requests>=2.28.0

# RSS parsing
feedparser>=6.0.11

# Language detection
langdetect>=1.0.9

# Database — async (ingestion pipeline)
greenlet==2.0.2
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0

# Database — sync (Streamlit dashboard)
psycopg2-binary>=2.9.9

# Schema migrations
alembic>=1.13.0

# Task scheduling
apscheduler>=3.10.4

# Dashboard — Phase 5A (Plotly Dash)
dash>=2.18
dash-bootstrap-components>=1.6
plotly>=5.22.0
pandas>=2.2.0

# Logging
loguru>=0.7.2

# NLP — Phase 3
nltk>=3.8
rapidfuzz>=3.0

# Entity Resolution Engine — spaCy NER (Phase 3 replacement)
# Model is downloaded automatically on first start if not already cached.
# To pre-bake into a Docker image, add to your Dockerfile:
#   RUN python -m spacy download en_core_web_sm
spacy>=3.7.0

# Sentiment analysis — Phase 4
google-genai>=1.0.0
pysentiment2>=0.1.1

# FinBERT sentiment engine
# For Railway CPU-only deployments, add this line ABOVE these two to halve
# the image size (~200 MB vs ~700 MB):
#   --extra-index-url https://download.pytorch.org/whl/cpu
transformers>=4.40.0
torch>=2.2.0

# Timezone handling
pytz>=2024.1

# WSGI server (Railway / production)
gunicorn>=21.0.0
